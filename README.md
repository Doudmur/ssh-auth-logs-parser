# SSH authentication logs parser
I created this project by real case from work. I noticed that our NTA(Network Traffic Analyzer) system, which get info about 
authentication events. However, after working with one linux machine, I found that Windows Log Collector tries to authenticate 
on this machine through SSH, but by the logic should not do this. So, I implemented this project to this machine.
